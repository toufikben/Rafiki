# تقرير الاختبارات والتحقق من Batch 10

**المشروع:** Rafiq / Rafiki  
**المستودع:** `toufikben/Rafiki`  
**تاريخ التحقق:** 22 سبتمبر 2026  
**نطاق التقرير:** الاختبارات الآلية الحالية، نتائج التحليل والبناء، وما تم إنجازه والمتبقي من Batch 10

## 1. الخلاصة التنفيذية

الحالة الحالية مستقرة من ناحية التحليل والاختبارات الآلية. نفذ المستودع **27 اختبارًا ناجحًا من أصل 27**، ونجح `flutter analyze` دون أي مشكلة. كما نجح بناء APK debug من حالة Gradle نظيفة بعد تجهيز Flutter وDart وAndroid SDK وJDK في بيئة التحقق.

تغطي اختبارات Batch 10 الحالية السلوك الحيوي الأساسي، وحدود القيم، أولوية الحالات المزاجية، حفظ ملف التعلم المحلي واستعادته، التعامل مع البيانات الفاسدة، تقارب المكافآت، انتقالات حركات الكلب، والتحقق من عقد Manifest الخاص بالنموذج. هذه التغطية تثبت صحة منطق محدد وقابل للتكرار، لكنها لا تغلق بعد بوابات الأداء أو الاختبار البصري على جهاز Android حقيقي.

> **النتيجة الأساسية:** Batch 10 منفذة جزئيًا بنجاح على مستوى الاختبارات الحتمية. أما قياس الأداء الفعلي، والصوت على جهاز، وذاكرة النموذج المحلي، واختبار الاستئناف، واختبار Android المادي، فما زالت بوابات مستقلة.

## 2. بيئة التحقق

| العنصر | الإصدار أو الحالة |
|---|---|
| Flutter | 3.47.5، القناة المستقرة |
| Dart | 3.13.4 |
| Java/JDK | OpenJDK 21.0.12.1 مع `javac` متاح |
| Android SDK | Platform 36 |
| Android Build Tools | 36.0.0 |
| Android NDK | 28.2.13676358 |
| Gradle | 8.14 |
| Android Gradle Plugin | 8.11.1 |
| Kotlin Gradle Plugin | 2.2.20 |
| APK الناتج | `build/app/outputs/flutter-apk/app-debug.apk` |
| حجم APK debug | 279 MB |
| SHA-256 للـAPK | `45c7c0d3d6a6cb60000938d560c8d52959cc5ea299ad5ea1e9810b9573d6dc59` |

أُنشئت ملفات البيئة المحلية مثل `android/local.properties` وملفات Gradle الوسيطة خارج الملفات المتتبعة في Git. لا تدخل هذه الملفات ضمن مصدر التطبيق أو ضمن نتيجة الاختبارات البرمجية.

## 3. بوابات التحقق المنفذة

| البوابة | الأمر أو الدليل | النتيجة |
|---|---|---|
| تحليل Dart وFlutter | `flutter analyze` | ناجح، دون مشكلات |
| الاختبارات الآلية | `flutter test -j 1` | ناجح، 27 من 27 |
| توليد التوطين | `flutter gen-l10n` | ناجح |
| توليد ملفات Isar | `dart run build_runner build --delete-conflicting-outputs` | ناجح، 116 مخرجًا |
| صحة Manifest | `jq empty assets/models/dog/dog_rig_manifest.json` | ناجح |
| سلامة الفروقات | `git diff --check` | ناجح |
| بناء Android debug | `flutter build apk --debug` | ناجح بعد تنظيف Gradle |
| اختبار Android مادي | جهاز Android فعلي | لم يُنفذ في هذه البيئة |
| قياس FPS والذاكرة والبطارية | profiling ميداني | لم يُنفذ بعد |

ظهرت أثناء البناء تحذيرات مستقبلية من Flutter بشأن إصدارات Gradle وAndroid Gradle Plugin وKotlin. لم تمنع هذه التحذيرات البناء الحالي، ولم تُجرَ ترقية عشوائية لها لأنها قد تغير توافق المشروع والمكونات الأصلية. ينبغي التعامل معها في مهمة مستقلة مع اختبار كامل بعد كل ترقية.

## 4. توزيع الاختبارات الحالية

يوجد خمسة ملفات اختبار بإجمالي 27 اختبارًا:

| الملف | المجال | عدد الاختبارات | النتيجة |
|---|---|---:|---|
| `test/needs_system_test.dart` | الاحتياجات والحيوية والمزاج | 7 | 7 ناجحة |
| `test/learning_profile_test.dart` | التعلم المحلي والتسلسل JSON | 4 | 4 ناجحة |
| `test/dog_animation_controller_test.dart` | اختيار الحركة وعقد النموذج | 5 | 5 ناجحة |
| `test/evolution_system_test.dart` | التطور والخبرة والحجم | 3 | 3 ناجحة |
| `test/batch10_closeout_test.dart` | cooldown الصوت، fallback، Isar persistence | 8 | 8 ناجحة |
| **المجموع** |  | **27** | **27 ناجحة** |

الاختبارات حتمية في المقام الأول. بعض وظائف التطبيق الإنتاجية مثل الوقت الحالي وطبقة الصوت وواجهات Flutter الأصلية لا تُحاكى بالكامل في هذه المجموعة، ولذلك لا ينبغي تفسير نجاحها على أنه إثبات لسلوك الجهاز أو واجهة النظام.

## 5. نتائج `NeedsSystem`

يطبق `NeedsSystem` نموذجًا مترابطًا للقيم الحيوية بدل التعامل مع كل شريط بصورة مستقلة. يتأثر الضغط بالجوع والترطيب والنظافة والدين النوم، بينما تتأثر السعادة بمجموع الرفاهية والضغط.

### 5.1 الاختبارات المنفذة

**انخفاض الجوع مع مرور الوقت.** يثبت الاختبار أن تحديثًا لمدة ساعة يخفض قيمة الجوع عندما تبدأ عند الحد الأعلى.

**عدم تغيير الحالة عند مدة صفرية.** يثبت الاختبار أن `Duration.zero` لا يغير الجوع أو الطاقة أو الترطيب أو `lastUpdated`. هذا يحمي التطبيق من تحديثات وهمية أثناء الإيقاف المؤقت أو إعادة البناء غير المقصودة.

**التغذية تمنح أثرًا وظيفيًا وخبرة.** يثبت الاختبار أن التغذية ترفع الجوع وتضيف خمس نقاط خبرة. وهو يختبر النتيجة الأساسية للتفاعل وليس مجرد استدعاء الدالة.

**بقاء القيم ضمن المجال الطبيعي.** يمرر الاختبار قيمًا كبيرة إلى التغذية والشرب والراحة، ثم يتحقق من بقاء الجوع والترطيب والطاقة والسعادة والنظافة والعاطفة والضغط ودين النوم ضمن المجال `0..1`. هذا يغطي خطر تجاوز الحدود عند تكرار التفاعل أو استقبال قيم غير مثالية.

**أولوية حالة الجوع.** يبدأ الاختبار بقيم قريبة من عتبة الجوع والترطيب، ثم يمرر ساعة. النتيجة المتوقعة هي `hungry`، ما يثبت أن الحالة الحيوية تنتقل إلى المزاج المناسب.

**أولوية النوم عند انخفاض الطاقة.** يثبت الاختبار أن الطاقة المنخفضة تنتج `sleepy` حتى عندما تكون السعادة مرتفعة نسبيًا. هذا مهم لأن قيود الرفاهية يجب أن تتقدم على الإشارات السلوكية الإيجابية.

**اكتشاف الحزن.** يثبت الاختبار أن انخفاض السعادة مع تدهور النظافة والعاطفة ينتج `sad` بعد التحديث.

### 5.2 ما يثبته المجال الحالي

تثبت هذه المجموعة أن القيم لا تتجاوز الحدود، وأن الزمن الموجب يغير الحيويات وفق النموذج، وأن الزمن الصفري لا يسبب طفرة، وأن ترتيب أولوية المزاج يعمل في الحالات المختبرة.

### 5.3 ما لا تثبته بعد

لا تغطي المجموعة بعد مقاومة القيم السالبة أو `Duration` الكبيرة جدًا على مدى أشهر، ولا تختبر استعادة الحالة بعد عملية ترحيل Isar، ولا تتحقق من تطابق النتائج عبر عدة تشغيلات عندما تدخل طبقة التخزين أو جدولة التطبيق في المسار.

## 6. نتائج `LearningProfile`

يخزن `LearningProfile` المكافآت محليًا داخل `PetState.learningProfileJson`. التعلم تدريجي ومحدود، ولا يفترض أن يحل محل قيود الجوع أو الإرهاق.

### 6.1 الاختبارات المنفذة

**دورة الحفظ والاستعادة.** يتعلم الملف تفضيلًا إيجابيًا للعب وآخر سلبيًا للمشي، ثم يكتبه إلى `PetState` ويعيد قراءته. يتحقق الاختبار من عدد العينات، وإشارة المكافأة، وإصدار JSON.

**البيانات الفاسدة والقديمة.** يمرر الاختبار JSON غير صالح وبنية JSON قديمة لا تحتوي على `values`. في الحالتين يعود النظام إلى ملف فارغ بدل رمي استثناء أو إيقاف التطبيق.

**حدود المكافأة والتقارب.** يرسل الاختبار مكافأة كبيرة بصورة متكررة. النتيجة يجب أن تبقى ضمن `[-1, 1]`، ويجب أن يبقى bonus السلوك ضمن الحد المتوقع. هذا يثبت أن قيمة الإدخال لا تستطيع تضخيم السياسة بلا سقف.

**السلوك الافتراضي للملف الفارغ.** يثبت الاختبار أن الملف الجديد يعطي `play` كتفاعل مفضل افتراضيًا، وأن bonus السلوك يساوي صفرًا قبل وجود عينات.

### 6.2 ما يثبته المجال الحالي

تثبت المجموعة سلامة دورة البيانات الأساسية: إنشاء، تعلم، حفظ، استعادة، رفض بيانات غير صالحة، وحدود التأثير السلوكي. كما تثبت أن التعلم لا ينتج قيمة غير محدودة.

### 6.3 ما لا تثبته بعد

لم تُختبر بعد منافسة عدة تفضيلات متساوية، أو ترحيل إصدار JSON من إصدار سابق فعلي، أو القراءة والكتابة المتزامنة مع Isar، أو تأثير الملف على توزيع قرارات `BehaviorEngine` عبر عينة طويلة. هذه اختبارات مناسبة للمرحلة التالية من Batch 10.

## 7. نتائج `DogAnimationController` وعقد النموذج

تفصل وحدة `DogAnimationController` اختيار الحركة عن الرسم. وهي تحول السلوك إلى اسم clip، وتحدد ما إذا كان clip حلقيًا، وتحدد زمن الانتقال، وتحفظ هدف الرجوع بعد الفعل القصير.

### 7.1 الاختبارات المنفذة

**ربط الحركة.** `walking` ينتج `Walk`، و`running` ينتج `Run`.

**أولوية الرفاهية.** الطاقة الأقل من العتبة تنتج `Sleep` حتى مع سلوك `running`. كما ينتج الضغط العالي أو السعادة المنخفضة `Sad`.

**استعادة الحركة بعد الفعل القصير.** عند الانتقال من `Run` إلى `Happy`، يسجل الاختبار `Run` كحركة سابقة وهدف استعادة. يمنع ذلك العودة الدائمة إلى `Idle` بعد كل فعل قصير.

**انتقال النوم.** يتحقق الاختبار من أن `Sleep` يستخدم انتقالًا أطول قدره 300 مللي ثانية وأنه حركة حلقية.

**Manifest contract.** يقرأ الاختبار `assets/models/dog/dog_rig_manifest.json` ويتحقق من وجود clips المطلوبة والمفاصل التي تعتمد عليها الحركة الثانوية.

### 7.2 حدود هذا التحقق

الاختبارات تتحقق من سياسة الاختيار والبيانات، لا من تشغيل clip داخل `flutter_scene`. ملف GLB الملتزم به حاليًا ما زال نموذجًا تجريبيًا ثابتًا، ولذلك لا يجوز اعتبار وجود أسماء clips في manifest دليلًا على وجود keyframes فعلية داخل GLB. قبول الحركات النهائي يحتاج ملف GLB إنتاجيًا، وفحص glTF، واختبارًا مرئيًا على Android.

## 8. نتائج `EvolutionSystem`

تغطي الاختبارات الأساسية انتقال الحيوان عند 150 نقطة خبرة، بقاء الحيوان في مرحلة الصغير تحت العتبة، وزيادة مقياس الرسم عند المرحلة الثالثة. هذه المجموعة صغيرة لكنها تغطي حدود التطور المعلنة حاليًا.

المجال المتبقي يشمل اختبار العتبة بدقة عند القيم القريبة منها، وتكرار الاستدعاء بعد التطور، وحدود المرحلة القصوى، وحفظ التطور واستعادته من التخزين.

## 9. تحليل فجوات Batch 10

| المجال | الحالة الحالية | مستوى الإثبات | الخطوة المطلوبة للإغلاق |
|---|---|---|---|
| اقتران الحيويات | منفذ ومختبر | اختبار وحدة | إضافة اختبارات زمنية طويلة وترحيل الحالة |
| أولوية المزاج | مختبرة للحالات الأساسية | اختبار وحدة | تغطية مصفوفة العتبات والتعادلات |
| التعلم والتقارب | منفذ ومختبر | اختبار وحدة | اختبار قرارات السلوك بعد عينات كثيرة |
| حالة الحركة | سياسة الاختيار مختبرة | اختبار وحدة | دمج المشغل مع `flutter_scene` واختبار GLB فعلي |
| عقد Manifest | متحقق آليًا | اختبار ملف | ربطه بفحص GLB الحقيقي لا بالـManifest فقط |
| الصوت والتبريد | منفذ في `AudioService` مع `AudioCooldownPolicy` | مختبر حتميًا | اختبار plugin latency على جهاز Android فعلي |
| fallback للمحادثة | مفصول في `ChatFallbackPolicy` ومختبر حتميًا | مختبر لغياب النموذج والاستجابات الآمنة | حقن فشل محرك النموذج والإلغاء وعدم التنزيل التلقائي |
| الحفظ والاستعادة | `Database` مع Isar واختبار إعادة الفتح والحذف | مختبر على Dart VM مع Isar Core | اختبار lifecycle على Android وترحيل schema قديم |
| الذاكرة والأداء | لم يُقاس | غير مثبت | profiling على أجهزة منخفضة ومتوسطة وعالية |
| FPS وGPU | لم يُقاس | غير مثبت | تسجيل frame time أثناء Idle وWalk وRun |
| البطارية | لم تُقَس | غير مثبت | جلسات تشغيل طويلة مع Android battery stats |
| الاستئناف والخلفية | لم يُختبر ميدانيًا | غير مثبت | اختبار lifecycle على جهاز Android فعلي |

## 10. خطة إغلاق مقترحة لبقية Batch 10

### المرحلة الأولى: إكمال الاختبارات الحتمية

أُنجزت هذه المرحلة عبر `AudioCooldownPolicy` بزمن قابل للحقن، واختبارات تثبت منع التكرار قبل الفاصل، استقلالية كل cue، وإعادة السماح بعد انتهاء الفاصل أو تنفيذ reset. كما فُصل fallback في `ChatFallbackPolicy` واختُبرت استجابات الجوع والماء باللغتين، إضافة إلى رفض التعامل مع نص تعليماتي كعملية تنفيذية. ما يزال حقن فشل محرك النموذج الحقيقي والإلغاء يحتاج adapter قابلًا للاستبدال عند توفر نموذج فعلي.

### المرحلة الثانية: اختبار الحالة والتخزين

أُنجزت تغطية الحفظ والاستعادة الأساسية عبر Isar Core على Dart VM. يفتح الاختبار قاعدة مؤقتة، يحفظ PetState مع learning JSON، يغلقها، يعيد فتحها، ثم يتحقق من الاسم والقيم وعدد العينات وإصدار JSON. كما يختبر `deleteAll`. المتبقي هو اختبار ترحيل `learningProfileJson` فعليًا من نسخة قديمة، والتعامل مع حقول مفقودة، وحفظ حالة التطور عبر lifecycle Android.

### المرحلة الثالثة: اختبار GLB الفعلي

عند وصول النموذج الإنتاجي، يجب فحص وجود المفاصل والـclips داخل GLB نفسه، وليس داخل manifest فقط. بعد ذلك يُختبر التحميل من `DogSceneView`، وfallback عند فقد الملف، وعدم تراكم دوران الحركة الثانوية، وعدم تغيير مقياس الجذر بسبب التحديثات المتكررة.

### المرحلة الرابعة: profiling على Android

يجب اختيار جهاز منخفض وجهاز متوسط وجهاز مرتفع نسبيًا. لكل جهاز تسجل النتائج التالية: زمن فتح المشهد، زمن ظهور أول إطار، متوسط وp95 للـframe time أثناء Idle وWalk وRun، استهلاك الذاكرة قبل وبعد تحميل المشهد، زمن استجابة الصوت، واستهلاك البطارية خلال جلسة ثابتة. يجب توثيق ظروف القياس وإصدار النظام وحجم الشاشة ومعدل التحديث.

### المرحلة الخامسة: تحديد ميزانيات قبول

لا يكفي وصف الأداء بأنه جيد. ينبغي اعتماد ميزانيات رقمية قبل الاختبار، مثل حد أقصى لزمن الإطار، وحد أقصى للذاكرة الإضافية بعد تحميل النموذج، وحد زمني للاستجابة الصوتية، وحد لحجم أصول Android. إذا فشل جهاز، يجب تحديد هل السبب هندسة GLB أو خامة أو shadow أو audio أو model runtime بدل تخفيض الجودة عشوائيًا.

## 11. القيود والقرارات المهنية

هذا التقرير لا يخلط بين نجاح الاختبارات البرمجية ونجاح تجربة المستخدم على الجهاز. `flutter analyze` يثبت سلامة التحليل الساكن، و`flutter test` يثبت النتائج المحددة في بيئة اختبار، ونجاح APK يثبت قابلية البناء. لا يثبت أي منها وحده جودة الرسوميات أو FPS أو استهلاك البطارية أو صحة الصوت عبر عتاد Android.

كما لا يعتبر وجود قائمة clips في `dog_rig_manifest.json` إثباتًا لوجود الحركات داخل `dog.glb`. الـManifest عقد قبول وتخطيط، بينما إثبات الأصل النهائي يحتاج فحص GLB ومراجعة مرئية وترخيصًا موثقًا.

## 12. الحالة النهائية الموصى بها

يمكن اعتبار الجزء الحالي من Batch 10 **مغلقًا على مستوى اختبارات الصوت وfallback والتخزين الحتمية الأساسية**، مع إبقائه **مفتوحًا على مستوى الأداء والاختبار الميداني وترحيل schema**. لا ينبغي تغيير الحالة إلى مكتمل بالكامل قبل تنفيذ profiling على أجهزة Android فعلية، وإضافة حقن فشل لمحرك النموذج، وتوثيق نتائج FPS والذاكرة والصوت والبطارية بأرقام قابلة للمقارنة.

## References

[1]: https://github.com/toufikben/Rafiki "Rafiq GitHub Repository"
[2]: https://docs.flutter.dev/testing "Flutter Testing Documentation"
[3]: https://docs.flutter.dev/perf/rendering-performance "Flutter Rendering and Performance Guide"
[4]: https://www.khronos.org/gltf/ "Khronos glTF Runtime 3D Asset Delivery"
