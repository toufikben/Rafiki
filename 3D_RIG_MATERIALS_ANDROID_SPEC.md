# المواصفة التقنية لأوزان العظام والخامات لنموذج Rafiq على Android

**النطاق:** نموذج الكلب ثلاثي الأبعاد النهائي في Batch 8  
**المحرك:** `flutter_scene` عبر Flutter GPU/Impeller  
**الصيغة:** glTF 2.0 Binary (`dog.glb`)  
**الهدف:** تشوهات مفاصل نظيفة مع أقل تكلفة ممكنة على وحدات GPU المحمولة

## 1. قواعد التصميم الأساسية

يجب التعامل مع النموذج باعتباره أصلاً مخصصاً لشاشة هاتف وليس نسخة حاسوب مصغرة. جودة النتيجة تعتمد على وضوح silhouette وصحة التشوه أكثر من عدد المضلعات أو كثرة المؤثرات. لذلك توزع الميزانية على الوجه والمفاصل التي تتحرك فعلياً، وتُخفض التفاصيل في الأسطح المسطحة والمناطق التي لا تظهر من كاميرا التطبيق.

يجب أن تكون كل الخامات قابلة للتصدير داخل GLB، وأن تكون كل أوزان العظام محدودة ومطبّعة، وأن يمر الملف النهائي بفحص آلي قبل وضعه في `assets/models/dog/dog.glb`. لا يُقبل أصل يعتمد على ملفات texture خارجية أو على أسماء عظام مولّدة تلقائياً.

## 2. ميزانية النموذج المستهدفة

| البند | الهدف | الحد الأعلى | سبب الحد |
|---|---:|---:|---|
| مثلثات النموذج الكامل | 20,000–45,000 | 60,000 | تقليل تكلفة الرسم والذاكرة |
| عدد المواد الفعالة | 3–6 | 8 | تقليل تبديل حالات الرسم |
| خامة اللون الأساسية | 1024² أو 2048² | 2048² | كافية للعرض القريب دون تضخم الذاكرة |
| خامات إضافية | 512²–1024² | 3 خرائط لكل مادة | تقليل bandwidth على GPU |
| عظام الـSkinned Mesh | 35–70 | 80 | تقليل تكلفة مصفوفات العظام |
| أقصى عظام لكل vertex | 4 | 4 | يتوافق مع مسار glTF المحمول المعتاد |
| حجم GLB المضغوط | أقل من 12 MB | 20 MB | تقليل زمن التحميل والذاكرة |
| عدد كائنات Mesh القابلة للرسم | 4–8 | 12 | تقليل draw calls |
| مؤثرات شفافة | للعين واللسان فقط | 2 مواد | الشفافية مكلفة وقد تسبب ترتيباً غير مستقر |

إذا ظهرت مشكلة أداء، تُخفض الخامات والتفاصيل غير المرئية قبل تخفيض جودة الوجه أو سلامة مفاصل الأرجل. لا تستخدم مؤثرات فرو حقيقية أو شعرات منفصلة في النسخة الأولى؛ فهي غير مناسبة للميزانية الحالية.

## 3. تصميم الهيكل العظمي

يجب أن يكون هناك جذر واحد واضح باسم `RafiqDog_ROOT`. يتحكم الجذر في موضع النموذج واتجاهه، ولا يدخل في skin deformation إلا إذا كان ذلك ضرورياً. تُستخدم عظام الجسم للتشويه، وتُستخدم عظام التحكم غير المشوّهة فقط داخل ملف المصدر إذا احتاجت عملية التأليف إليها، ثم تُزال أو تُخبز قبل التصدير.

### 3.1 الهيكل المطلوب

```text
RafiqDog_ROOT                  # موضع واتجاه النموذج
└── Pelvis                      # مركز وزن الجسم الخلفي
    ├── Spine_01
    │   └── Spine_02
    │       └── Chest           # مطلوب للحركة الثانوية
    │           ├── Neck
    │           │   └── Head
    │           │       ├── Jaw
    │           │       ├── Muzzle
    │           │       ├── Ear_L  # مطلوب للحركة الثانوية
    │           │       └── Ear_R  # مطلوب للحركة الثانوية
    │           ├── Shoulder_L
    │           └── Shoulder_R
    ├── Hip_L
    ├── Hip_R
    └── Tail_01                  # مطلوب للحركة الثانوية
        └── Tail_02              # مطلوب للحركة الثانوية
            └── Tail_03          # مطلوب للحركة الثانوية
```

وتتفرع من الكتفين والوركين عظام الأرجل:

```text
Shoulder_L → UpperArm_L → Forearm_L → PawFront_L
Shoulder_R → UpperArm_R → Forearm_R → PawFront_R
Hip_L      → Thigh_L    → Shin_L    → PawBack_L
Hip_R      → Thigh_R    → Shin_R    → PawBack_R
```

يمكن إضافة `ToeFront_L/R` و`ToeBack_L/R` إذا أثبتت الحركات الحاجة إليها، لكن لا تُضاف عظام منفصلة لكل إصبع في النسخة المحمولة. يفضل استخدام شكل paw عريض مع تشوه بسيط بدلاً من زيادة عدد العظام.

### 3.2 قواعد المحاور والتحويلات

يجب تطبيق transforms على الـMesh والـArmature قبل التصدير. يكون اتجاه الوجه نحو `+Z`، ويكون المحور `Y` هو الارتفاع، ويكون سطح الأرض عند `Y=0`. يجب أن تكون scale المحلية للعظام مساوية تقريباً لـ`1,1,1` بعد التطبيق، وألا يحتوي الملف على scale سالب أو mirrored transform.

يجب أن يكون موضع الأصل عند مركز الجسم أو عند نقطة ارتكاز ثابتة معروفة. لا يوضع الأصل تحت رأس الحيوان أو خارج جسمه، لأن ذلك يصعب حساب الكاميرا والانتقال لاحقاً. يجب توثيق المسافة من الأصل إلى الأرض ومن الأصل إلى مركز الصدر داخل `dog_manifest.json`.

## 4. مواصفات أوزان العظام

### 4.1 قواعد عامة لكل vertex

كل vertex يجب أن يملك وزناً موجباً لعظمة واحدة على الأقل. مجموع أوزان كل vertex يجب أن يساوي `1.0` بعد التطبيع ضمن هامش `±0.001`. لا يتجاوز عدد التأثيرات المؤثرة في vertex الواحد أربعة؛ ويفضل تأثيران أو ثلاثة في الأسطح البسيطة.

تُحذف الأوزان الأقل من `0.01` بعد المراجعة، إلا إذا كان حذفها يسبب خطاً مرئياً في انتقال الرقبة أو الكتف. لا تُترك مجموعات عظام فارغة في الملف النهائي، ولا تُنشأ أوزان لعظام تحكم غير موجودة في GLB.

### 4.2 توزيع الأوزان حسب المنطقة

| المنطقة | العظام المسموحة | السياسة المقترحة |
|---|---|---|
| مقدمة الرأس | `Neck`, `Head` | الرأس 0.85–1.0، انتقال الرقبة 0.0–0.15 |
| قاعدة الرأس | `Neck`, `Head`, `Chest` | ثلاثة تأثيرات ناعمة، دون قفزة حادة |
| الكمامة | `Head`, `Muzzle`, `Jaw` | الكمامة مرتبطة بالرأس غالباً؛ الفك يستخدم فقط لأسفل الفم |
| الأذن | `Head`, `Ear_L/R` | قاعدة الأذن مشتركة، الطرف 0.85–1.0 للأذن |
| الصدر | `Chest`, `Spine_02` | الصدر 0.65–0.85، العمود 0.15–0.35 |
| البطن | `Spine_01`, `Spine_02`, `Pelvis` | انتقال متدرج، لا يزيد أي مفصل منفرد عن 0.7 قرب المركز |
| الحوض | `Pelvis`, `Thigh_L/R` | الحوض يملك معظم الوزن؛ الفخذ يسيطر على منطقة الفخذ فقط |
| الكتف الأمامي | `Chest`, `Shoulder_L/R`, `UpperArm_L/R` | ثلاثة تأثيرات كحد أقصى لمنع الطي الحاد |
| الساق العلوية | `UpperArm` أو `Thigh` | 0.75–1.0 للعظمة المحلية مع انتقال قصير عند المفصل |
| الساق السفلية | `Forearm` أو `Shin` | لا تتجاوز عظمة الجار 0.2 قرب المفصل |
| الكفوف | `PawFront` أو `PawBack` | 0.85–1.0، مع انتقال محدود من الساق |
| الذيل | `Tail_01/02/03` مع الأب | قاعدة الذيل مشتركة، والطرف يتبع العظمة المحلية بنسبة أعلى |
| العين والأنف | `Head` أو `Muzzle` | 1.0 لعظمة واحدة، إلا إذا كان الجزء يتبع الفك |

الأرقام السابقة نقاط بداية للفنان وليست أوزاناً آلية نهائية. يتم تعديلها بالـpose tests، خصوصاً عند ثني الكتف والورك والرقبة. المعيار النهائي هو غياب التشوه المرئي، لا مطابقة رقم محدد.

### 4.3 توزيع الذيل والأذنين

يجب أن تكون قاعدة الذيل مرتبطة بمزيج من `Pelvis` و`Tail_01` حتى لا تنفصل عن الجسم. يزداد تأثير `Tail_02` و`Tail_03` تدريجياً باتجاه الطرف. لا ينبغي أن يتأثر طرف الذيل بـ`Chest` مباشرة؛ ذلك يسبب اهتزازاً غير طبيعي عند التنفس.

ترتبط قاعدة كل أذن بمزيج من `Head` و`Ear_L/R`، بينما يرتبط الطرف بعظمة الأذن بنسبة أعلى. يجب ترك مساحة كافية للحركة الدورانية التي يستخدمها `DogSecondaryMotion` حول محور Z. إذا كان شكل الأذن صلباً، يُفضل تقليل الحركة الإجرائية بدلاً من تشويهها بأوزان حادة.

### 4.4 اختبار جودة الأوزان

يجب إنشاء مجموعة poses ثابتة قبل التصدير:

1. دوران الرأس إلى اليمين واليسار مع ثبات الصدر.
2. خفض الرأس إلى وضعية الأكل والشرب.
3. رفع الكتف ومد الساق الأمامية.
4. ثني الورك ورفع الساق الخلفية.
5. انحناء الظهر في الجري.
6. التفاف الذيل إلى الجانبين.
7. خفض الأذنين ثم رفعهما.

تُرفض الأوزان إذا ظهرت ثقوب بين الرأس والرقبة، أو انكماش حاد في الكتف، أو انثناء غير طبيعي للكف، أو تمدد الذيل، أو تحرك العين خارج محجرها. يجب حفظ لقطات هذه الاختبارات مع رقم إصدار الـRig.

## 5. مواصفات الخامات والمواد

### 5.1 نموذج PBR الأساسي

يستخدم النموذج مواد PBR بسيطة تعتمد على اللون الأساسي والخشونة. الهدف هو الحفاظ على مظهر كرتوني غني دون shader معقد أو خرائط كثيرة. القيم التالية نقطة البداية:

| المادة | Base Color | Metallic | Roughness | ملاحظات |
|---|---|---:|---:|---|
| الفرو الأساسي | حسب لوحة الألوان المعتمدة | 0.0 | 0.72–0.9 | لا يستخدم metallic للفرو |
| الكمامة/البطن الفاتح | لون فاتح متناسق | 0.0 | 0.78–0.92 | يمنع لمعاناً بلاستيكيّاً |
| الأنف | بني أو أسود داكن | 0.0 | 0.28–0.48 | لمعان محدود وليس مرآة |
| العين | أسود داكن | 0.0 | 0.18–0.32 | يفضل جسم عين غير شفاف |
| اللسان | وردي أو أحمر هادئ | 0.0 | 0.52–0.7 | لا يحتاج translucency في النسخة الأولى |
| الأرض | بني/رمادي دافئ | 0.0 | 0.9–1.0 | يستقبل الظلال ولا يلفت الانتباه |

لا تستخدم قيمة metallic أعلى من `0.05` لأي جزء من الحيوان. المعدن غير مناسب للفراء والجلد، ويزيد حساسية المشهد للإضاءة دون فائدة بصرية.

### 5.2 الخرائط المسموحة

النسخة القياسية تستخدم خرائط قليلة:

- **Base Color:** خريطة sRGB مضغوطة أو لون مضمّن حسب حجم التفاصيل.
- **Normal:** اختيارية، وتستخدم فقط للفرو أو تفاصيل الأنف التي تظهر بوضوح.
- **Roughness/Metallic:** خريطة خطية واحدة مدمجة عند الحاجة، مع Metallic قريب من الصفر.
- **Occlusion:** اختيارية إذا كانت تكلفة الذاكرة مبررة؛ البيئة توفر AO على مستوى المشهد.
- **Emission:** غير مستخدمة في الحيوان إلا لضرورة واضحة مثل لمعة عين مضبوطة.

لا تُستخدم خرائط displacement أو tessellation أو parallax في النسخة المحمولة الأولى. تفاصيل الفرو تُعبّر عن طريق normal خفيف أو تدرج اللون، وليس عبر هندسة شعر منفصلة.

### 5.3 ألوان ومساحات التحويل

يجب إعداد Base Color في مساحة sRGB، بينما تُعامل Roughness وMetallic وNormal وOcclusion كبيانات خطية. لا تُحفظ خريطة Normal كصورة sRGB. يجب تثبيت اتجاه قنوات Normal وفحصه داخل Android؛ انقلاب القناة الخضراء يظهر غالباً كإضاءة مقلوبة على الوجه.

يفضل استخدام PNG أو WebP مضغوط للخرائط التي تحتاج شفافية أو حوافاً حادة، واستخدام تنسيق مناسب للهاتف للخرائط غير الشفافة بعد اختبار أن `flutter_scene` يحمّلها من GLB دون أخطاء. يجب ألا تعتمد الخامة على sampler أو extension غير مدعوم في مسار التطبيق.

### 5.4 توزيع UV

يُخصص أكبر جزء من مساحة UV للوجه، والعينين، والأذنين، والكمامة. تحصل الأرجل والجسم على مساحة متناسبة مع ظهورها في الكاميرا. يجب تجنب الجزر الصغيرة الكثيرة لأنها تزيد الحواف والفراغات وتقلل كفاءة الخامة.

يجب توحيد اتجاه texel density بين الرأس والجسم. لا تُستخدم خامة 2048² إذا كان الوجه لا يشغل إلا جزءاً صغيراً منها؛ في هذه الحالة تكون 1024² أفضل للذاكرة وزمن التحميل.

## 6. تقليل تكلفة GPU والذاكرة

### 6.1 تقليل draw calls

تُدمج الأجزاء التي تشترك في المادة داخل Mesh واحد متى لم تكن تحتاج حركة مستقلة. لا تُدمج الأذن أو الذيل مع الجسم إذا كان ذلك يمنع الـSkinning أو الحركة الإجرائية. الهدف العملي هو إبقاء عدد كائنات الرسم ضمن 4–8، مع وجود سبب واضح لكل مادة إضافية.

تُتجنب المواد الشفافة. العين واللسان يستخدمان هندسة صغيرة غير شفافة قدر الإمكان، لأن الشفافية تفرض ترتيباً إضافياً وقد تسبب تداخلات على الأجهزة المحمولة.

### 6.2 ضغط الأصول

بعد اعتماد النموذج، تُجرى نسخة مضغوطة وتجربة تحميل على جهاز Android. يجب قياس الحجم قبل وبعد الضغط، ومقارنة الصورة في الوجه والألوان. لا يتم ضغط الـNormal أو الخامات الصغيرة بطريقة تسبب banding أو عيوباً واضحة حول العين والكمامة.

يمكن استخدام quantization أو ضغط mesh إذا كان مسار `flutter_scene` المستخدم في المشروع يدعمه دون فقد أسماء العظام أو الحركات. أي extension جديد يجب اختباره على نسخة APK فعلية؛ وجوده في ملف GLB لا يعني أن runtime الحالي سيطبقه.

### 6.3 الذاكرة

يجب حساب الذاكرة التقريبية لكل خامة، مع افتراض أن الصورة قد تُفك إلى صيغة GPU أكبر من حجمها المضغوط على القرص. لذلك لا يعتمد قرار 2048² على حجم الملف فقط. إذا ظهرت قفزات في الذاكرة عند فتح المشهد، تُخفّض دقة الخرائط أولاً وتُزال الخامات غير المرئية.

لا تُحمّل نسخ متعددة من النموذج في الذاكرة. يجب أن يظل هناك instance واحد من GLB في `DogSceneView`، وأن يُعاد استخدام الموارد عند العودة إلى الصفحة إن كان lifecycle للمحرك يسمح بذلك.

## 7. توافق الخامات مع البيئة الحالية

بيئة `DogEnvironment` تستخدم إضاءة اتجاهية، ظلالاً، AO، ضباباً وغباراً. لذلك يجب ألا تكون الخامة مظلمة جداً من البداية؛ فالـAO والضباب سيخفضان الإضاءة مرة أخرى. يتم ضبط الفرو تحت الإضاءة الفعلية للمشهد لا تحت HDRI منفصل فقط.

يجب اختبار ثلاث حالات إضاءة:

1. الإضاءة الافتراضية الدافئة مع الظلال.
2. تعطيل AO للتأكد من أن الخامة لا تعتمد عليه لإظهار التفاصيل.
3. جهاز منخفض الأداء مع تقليل الظلال أو الغبار.

يُرفض أي لون لا يمكن تمييزه عند تعطيل أحد مؤثرات البيئة. الخامة الجيدة تحتفظ بقراءة الشكل حتى عند خفض المؤثرات.

## 8. فحص آلي قبل اعتماد GLB

يجب أن ينفذ كل إصدار من الأصل فحوصاً مشابهة للقائمة التالية:

```text
[ ] الملف موجود في assets/models/dog/dog.glb
[ ] الاتجاه +Z والارتفاع Y
[ ] الأرض عند Y=0 والقدمون لا يغرقون
[ ] لا توجد transforms سالبة أو scale غير مطبق
[ ] جميع vertices لها مجموع أوزان = 1 ± 0.001
[ ] أقصى عدد influences لكل vertex هو 4
[ ] لا توجد عظام بأسماء تلقائية أو مجموعات فارغة
[ ] العقد Chest وEar_L وEar_R وTail_01..03 موجودة
[ ] الحركات Idle وWalk وRun وPlay وSleep وEat وDrink وHappy وSad موجودة
[ ] لا توجد خامة تشير إلى مسار خارجي مفقود
[ ] عدد المواد والمثلثات داخل الميزانية
[ ] لا توجد كاميرا أو أضواء غير مطلوبة داخل GLB
[ ] manifest يطابق رقم إصدار الأصل
```

يمكن تنفيذ الفحص عبر سكربت Python مستقل يقرأ glTF JSON من GLB. الفحص لا يقرر جودة الحركة بصرياً، لذلك يبقى pose test وAndroid QA إلزاميين.

## 9. اختبار الأداء على Android

يجب تسجيل النتائج على جهاز منخفض ومتوسط الأداء، وليس على المحاكي فقط. لكل جهاز تسجل القيم التالية:

| القياس | طريقة القياس | معيار القبول الأولي |
|---|---|---|
| زمن تحميل النموذج | من بدء `Node.fromGlbAsset` إلى أول إطار | لا توجد وقفة ملحوظة بعد فتح الصفحة |
| FPS أثناء Idle | شاشة المشهد دون تفاعل | قريب من معدل تحديث الجهاز |
| FPS أثناء Run والغبار | أعلى حمل متوقع | لا يوجد تقطع مستمر أو هبوط حاد |
| الذاكرة بعد فتح الصفحة | Android profiler أو adb | لا توجد زيادة مستمرة عند إعادة الدخول |
| زمن التفاعل | لمس أو بدء فعل إلى تغير الحركة | استجابة فورية تقريباً |
| الحرارة والبطارية | جلسة اختبار قصيرة متكررة | لا ارتفاع غير مبرر مقارنة بالمشهد الأساسي |

إذا فشل الاختبار، يكون ترتيب المعالجة: خفض الغبار، خفض shadow map، خفض دقة الخامات، تقليل المثلثات، ثم مراجعة عدد العظام. لا تُعطل الحركة الثانوية أولاً، لأنها جزء من جودة الحيوان ويمكن خفض شدتها دون حذفها.

## 10. سياسة الإصدارات والقبول

يُعطى كل أصل رقم إصدار مثل `dog-art-0.2.0`. يتغير الرقم عند تغيير الـRig أو أسماء الـclips أو توزيع الخامات. ويزداد رقم التصحيح عند إصلاح UV أو ضغط ملف دون تغيير العقد البرمجية.

يجب حفظ manifest مع:

```json
{
  "asset": "dog",
  "version": "dog-art-0.2.0",
  "axis": "forward:+Z, up:+Y",
  "triangles": 0,
  "materials": 0,
  "bones": 0,
  "max_influences": 4,
  "clips": ["Idle", "Walk", "Run", "Play", "Sleep", "Eat", "Drink", "Happy", "Sad"],
  "secondary_motion_nodes": ["Chest", "Ear_L", "Ear_R", "Tail_01", "Tail_02", "Tail_03"]
}
```

لا يُغلق Batch 8 إلا بعد نجاح فحص الملف، ونجاح `flutter analyze`، ونجاح الاختبارات، وبناء APK أو AAB، ومراجعة Android على جهازين. نجاح بناء APK وحده يثبت سلامة المشروع البرمجية، لكنه لا يثبت صحة الأوزان أو الخامات أو الأداء البصري.

## 11. التوصية التنفيذية الحالية

تبدأ المرحلة التالية بإنتاج نسخة Rig صغيرة على النموذج الأصلي الحالي، لا بانتظار اكتمال التفاصيل النهائية. الهدف من النسخة الصغيرة هو إثبات أسماء العظام، واختبار الجلد، وتأكيد أن `DogSecondaryMotion` لا يراكم الدوران عند تشغيل clip.

بعد اعتماد الـRig، تُنقل الخامات النهائية والحركات إلى نفس الهيكل دون تغيير أسماء العظام. هذا يقلل مخاطر إعادة كتابة كود Flutter. أي تغيير في أسماء العظام بعد بدء الحركات يجب اعتباره تغييراً عالي التكلفة ويحتاج قراراً موثقاً.

## References

[1]: https://www.khronos.org/gltf/ "Khronos glTF Runtime 3D Asset Delivery"
[2]: https://registry.khronos.org/glTF/specs/2.0/glTF-2.0.html "glTF 2.0 Specification"
[3]: https://docs.blender.org/manual/en/latest/addons/import_export/scene_gltf2.html "Blender glTF 2.0 Exporter Manual"
[4]: https://docs.flutter.dev/perf/rendering-performance "Flutter Rendering and Performance Guide"
[5]: https://github.com/toufikben/Rafiki "Rafiq GitHub Repository"

---

**القرار الفني:** اعتماد four-influences maximum، مواد PBR قليلة، خامات مضمنة، skeleton مسمى، وin-place locomotion في الإصدار الأول لتقليل تكلفة GPU والذاكرة وتعقيد الدمج داخل Android.
